# 03. 细节实现（已落地亮点对照表）


---

## 已落地亮点（三列对照）

| 亮点 | 如何实现（当前仓库落点） | 设计来源 |
|------|------------------------|-------------------------|
| **证据链 `evidence`** | `schemas.ProfileEvidence` + `GroupProfile.evidence`；`agents/profiler.py` 为 scene / 人数 / 饮食 / 兴趣等字段记录 `term`、`source`、`confidence`。 | `Profiler Agent 设计思路.md` |
| **历史偏好 `history_context`** | `state.history_context` → Profiler `_merge_history` → `history_weights`；高权重标签并入 `interests`；`demo --history` 注入 fixture。 | `Profiler Agent 设计思路.md` |
| **中文人数（一家三口等）** | `agents/profiler._PEOPLE_PHRASES` + 阿拉伯数字 + 单字中文，优先级高于场景默认。 | `Profiler Agent 设计思路.md`（§人数推理，本仓扩展短语表） |
| **可编辑画像标签** | `schemas.EditableTag` + `_build_editable_tags`（含 `utterance` / `history` 来源）。 | `Profiler Agent 设计思路.md` + `02.架构和agent.md` §3.2 |
| **Researcher 五维打分** | `agents/researcher.py`：preference 35% / history 20% / rating 20% / distance 15% / budget 10% → `POICandidate.breakdown`。 | `Researcher Agent 设计思路.md` §3–4 |
| **POI 检索 Mock HTTP** | `mock_meituan/catalog.py` + `GET /poi/search`；Researcher 经 `tools/http_client.search_poi`（内联 ASGI 或 `MOCK_MEITUAN_BASE_URL`）。 | `Researcher Agent 设计思路.md` + `planner/retriever.py`（HTTP 思路） |
| **Planner 硬约束过滤** | `agents/planner._passes_hard_filter`：亲子玩 / 低卡吃 / 距离；砍光则 soft fallback。 | `03-Planner+Scorer设计文档.md` Step 2 · `planner/filters.py` |
| **规则选店（可解释）** | Researcher 按 `breakdown.total` 排序；Planner 取过滤后首位；`Plan.score` = 各 stage primary 均分。 | `scoring/rules.py` + `Researcher Agent 设计思路.md` |
| **两种阶段顺序** | `_ORDER_VARIANTS`：`玩→吃` / `吃→玩`；`Plan.order_label`；加餐锚在「吃」+90min。 | `03-Planner+Scorer设计文档.md` Step 0（**规则 2 种**，非 LLM） |
| **Top-2 方案** | `build_plans(top_k=2)` → `plan` + `plan_alternatives`；notifier 行程卡「备选方案」。 | `03-Planner+Scorer设计文档.md` |
| **Tool 全链路 HTTP** | `registry.invoke` → `http_client` → `mock_meituan/routes`；DryRun / Executor / Compensator 同源。 | `planner/tool_hub.py` + `01-题目工程拆解.md` Mock API |
| **失败注入 + 补偿重规划** | `force_failure` → 写请求 `force_fail` → 409；`cancel_order` HTTP；Planner `blocked` 换备选 POI。 | `02.架构和agent.md` §7.3 · `nodes/compensator.py` |
| **Critic 规则校验** | `nodes/critic.py`：低卡 / 亲子 / 阶段完整；不通过回 Planner（未超迭代上限）。 | `nodes/critic.py` |
| **统一 trace** | `roles.trace_line`；四角色名与架构文档一致。 | `02.架构和agent.md` §7 · `planner/trace.py` |
| **Demo / 答辩开关** | `demo --scene` / `--fail 吃` / `--history`；打印 Mock 内联 vs TCP。 | 工程约定 · `docs/mock-api.md` |

---

## 未做

| 项 | 当前状态 | 原设计出处 |
|--------|----------|------------|
| LLM 选择阶段顺序 | 仅规则 2 序 | `03-Planner+Scorer` Step 0.5 · `planner/llm_wrapper.py` |
| LLM 地理顺路 / 凝聚力 | 未做 | `03-Planner+Scorer` Step 5 · `scoring/llm_judges.py` |
| LLM 两两比对方案 | 未做 | `03-Planner+Scorer` Step 6 |
| 多店组合 + 路程矩阵 | 未做 | `03-Planner+Scorer` Step 3 · `planner/composer.py` |
| ThreadPool 并行检索 | 未做 | `03-Planner+Scorer` Step 1 |
| Researcher 内再预检 | 集中在 `dry_run` | `Researcher Agent 设计思路.md`（与 DryRun 分工重复） |
| `user_confirmed` HIL 断点 | 仅 State 字段 | `02.架构` HIL · 需改图 + 前端 |
| 前端标签点改回写 | 未做 | `02.架构` ui_chips |
| 真实用户画像库 | `--history` fixture | `Profiler Agent` 历史 DB 设想 |

---


---

## 相关文档

| 文件 | 用途 |
|------|------|
| `02.架构和agent.md` | LangGraph 拓扑、节点职责、State 字段 |
| `docs/mock-api.md` | Mock 美团 HTTP 端点、curl、双进程演示 |
| `01-题目工程拆解.md` | 赛题与 Mock API 约定 |
