# 04. 第一阶段展示

> 本文档基于**真实跑通记录**整理。  
> 命令（项目根目录）：`python -m backend.demo --scene family`（主路径）  
> 补偿链：`python -m backend.demo --scene family --fail 吃`

---

## 一、你怎么跑（PyCharm 一句话）

底部 Terminal 已显示 `(.venv)` 且目录在项目根时，直接执行上面两条命令即可。  
首行 `Mock 美团：ASGI 内联（默认）` 表示 Tool 走 HTTP 协议、进程内 Mock，无需另开端口。

**带 UI 演示**：`python app.py` → 浏览器打开 `http://127.0.0.1:8000`

---

## 二、展示用输入

```text
今天下午是空的，想和老婆孩子出去玩几个小时，别离家太远，老婆最近在减肥，孩子 5 岁，帮我安排一下。
```

补偿链额外开关：`--fail 吃`（仅第一次下单时让「吃」阶段返回满座 409）。

---

## 三、全链路（对照终端每一行 trace）

```mermaid
flowchart LR
  IN[用户输入] --> P[Profiler]
  P --> R[Researcher 初搜]
  R --> PL[Planner]
  PL --> TR[TargetedResearcher 精准搜]
  TR --> C[Critic]
  C --> DR[DryRun 预检]
  DR --> EX[Executor 提交]
  EX -->|失败| CP[Compensator 回滚]
  EX -->|全成功| N[Notifier]
  CP --> PL2[Planner 重规划]
  PL2 --> C2 --> DR2 --> EX2 --> N
```

---

## 四、主路径实测：`demo --scene family`

### 4.1 典型终端输出

```text
Mock 美团：ASGI 内联（默认）　可设 MOCK_MEITUAN_BASE_URL 切换

╭─ 用户输入 ─╮
│ 今天下午是空的，想和老婆孩子出去玩几个小时，别离家太远，老婆最近在减肥，孩子 5 岁，帮我安排一下。 │
╰─────────────╯

── 执行链路 ──
  [Profiler] scene=family(conf=0.92) people=3 start=14:00 dietary=['低卡'] interests=['亲子']
  [Researcher] 初搜 阶段 2，候选 5 项｜玩=奥林匹克森林公园，吃=Wagas 沙拉轻食（奥森店）
  [Planner] 首次规划：家庭周末（玩 → 吃）… score=0.78；备选 …
  [TargetedResearcher] 精准搜 2 项｜加餐=原麦山丘 小蛋糕（送至餐厅）
  [Planner·校验] approved=True issues=0 ✓
  [Executor·预检] 打听 N 项：N 可执行 ✓
  [Executor·提交] 成功 N 笔 ✓
  [Executor·交付] 行程卡已生成 ✓

── 最终行程卡 ──
  （Markdown 行程表 + 微信 share_text）

── State 摘要 ──
  scene=family, evidence_count=8, plan_iteration=1, executed=N, failed=0
```

> 订单号 `M52ECD2C220` 等每次运行随机，**有订单号即表示 HTTP 下单成功**。

### 4.2 执行链路 × 亮点对照表

| 终端 trace 行 | 对应节点 | **亮点** |
|---------------|----------|----------|
| `[Profiler] scene=family … dietary=['低卡']` | `profiler` | 画像 + 证据链，从自然语言提取场景/人数/饮食约束 |
| `[Researcher] 初搜 …` | `researcher` | Mock HTTP 检索 + 五维打分，搜吃/玩 |
| `[Planner] 首次规划 … score=…` | `planner` | 硬过滤 + 双顺序 + Top2 备选 |
| `[TargetedResearcher] 精准搜 …` | `targeted_researcher` | 按规划顺序搜加餐/奶茶等顺路活动 |
| `[Planner·校验] approved=True` | `critic` | 规则校验闸门（低卡须轻食、亲子须友好） |
| `[Executor·预检]` | `dry_run` | 先打听再买 |
| `[Executor·提交]` | `executor` | 真 HTTP 下单，回执 order_id |
| 行程表 + 备选方案 | `notifier` | Markdown 行程卡 + 微信文案 |

---

## 五、加分路径：`demo --scene family --fail 吃`

| 对比项 | 主路径 | 补偿链 |
|--------|--------|--------|
| 餐厅 | Wagas | **新元素轻食**（下一候选） |
| trace | 正常闭环 | 多「回滚 + 重规划 + 第二轮提交」 |
| `plan_iteration` | 1 | **≥2** |
| 亮点 | 正常闭环 | 故障注入 → 回滚 → 避开失败 POI → 再成功 |

---

## 六、六大亮点总表

| # | 亮点 | 代码落点 |
|---|------|----------|
| 1 | 可解释画像 | `backend/agents/profiler.py` |
| 2 | 五维 POI 打分 | `backend/agents/researcher.py` |
| 3 | 规划智能感 | `backend/agents/planner.py` + `planner/` |
| 4 | Mock HTTP Tool | `backend/mock_meituan/` + `tools/http_client.py` |
| 5 | 校验闸门 | `backend/nodes/critic.py` |
| 6 | 补偿自愈 | `backend/nodes/compensator.py` |

---

## 七、自查清单

- [x] `python -m backend.demo --scene family` 跑通
- [x] `python -m backend.demo --scene family --fail 吃` 跑通
- [ ] `python app.py` + 浏览器 UI 演示
- [ ] （可选）`python -m backend.demo --history` 历史偏好

---

## 八、相关文档

| 文档 | 用途 |
|------|------|
| `02.架构和agent.md` | 节点职责、State、路由 |
| `03.细节实现.md` | 亮点与设计来源对照 |
| `docs/mock-api.md` | Mock HTTP curl 演示 |
